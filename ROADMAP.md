# 完整追平差距

0.5.0 已完成 IDL/schema、MoonBit 类型/RPC 绑定、通用 Node framed TCP/TLS/mTLS、multiplex 和独立双向互通增量。仍需推进：

1. 完整上游 IDL 语料、诊断位置/文本、所有注解与文档元数据、include 同名 stem 的生成处理、各语言默认值和集合差异。
2. JSON/Header 等协议、unframed/HTTP transport、SASL，以及其他生成语言和更广泛跨语言/跨主机互通。
3. 大规模连接、长期负载、断线/背压压力、代表性吞吐和内存对照；目前只有有界本机功能检查和样例计时。
4. 固定并实测工具链版本、更新正式申报与全部归档；旧 ZIP/bundle 不能当作当前源代码。
5. 只有用户另行授权后才考虑远端发布。当前独立仓库无 remote，CI 未远程运行。

上述剩余工作继续计入用户的 20 项追平目标，不因本轮测试通过而删减。
